# Tests

Put tests and fixtures here.

For this Bash-focused project, prefer small tests around pure helper functions
and smoke checks for sourceability. Interactive `fzf` behavior should be tested
manually until a stable non-interactive harness exists.
